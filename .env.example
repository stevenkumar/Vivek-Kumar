# ==============================================================================
# VERCEL ENVIRONMENT VARIABLES - vivekkumar portfolio (file-based backend, no DB)
# ==============================================================================
# Add these to Vercel → Project → Settings → Environment Variables, OR
# copy to a local .env.production.local for production testing.

# ── Frontend / Client ─────────────────────────────────────────────────────────
CLIENT_URL=https://your-portfolio-domain.vercel.app

# ── Email notifications (Nodemailer, Gmail SMTP) ──────────────────────────────
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your_16_char_gmail_app_password
EMAIL_TO=your-email@gmail.com

# ── Admin password (optional — otherwise uses backend/api/data/settings.json) ─
# ADMIN_PASSWORD=Admin123!
