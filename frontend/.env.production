# Frontend Environment Variables
# This file is used during Vercel build process

# API Base URL - Empty means use relative URLs (same origin)
# In production on Vercel, /api/* routes are rewritten to serverless functions
VITE_API_BASE_URL=

# Application Environment
VITE_APP_ENV=production

# Enable source maps in production for debugging
VITE_SOURCE_MAP=false
